name: tap_water_safety
description: App search fluid transition.
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  auto_size_text: ^2.1.0
  get_it: ^4.0.1
  provider: ^4.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  lint: ^1.1.1

# For information on the generic Dart part of this file, see the
# following page: https://www.dartlang.org/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  uses-material-design: true
